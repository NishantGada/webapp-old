name: Webapp Test workflow

on:
  pull_request:
    branches:
      - main

jobs:
  terraform:
    name: Initiating Webapp Checks…
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4